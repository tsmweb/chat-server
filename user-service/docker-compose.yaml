version: "3"

services:
  app:
    container_name: user-service
    build: .
    ports:
      - "8082:8082"
    volumes:
      - .:/go/src/
    environment:
      SERVER_PORT: 8082
      PGHOST: 172.17.0.1
      PGPORT: 5432
      PGDATABASE: chat_db
      PGSCHEMA: chat_db
      PGUSER: salesapi
    extra_hosts:
      - "host.docker.internal:172.17.0.1"
